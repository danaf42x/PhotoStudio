<UserControl x:Class="PhotoStudio.PL.Pages.Sub.OrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PhotoStudio.PL.Pages.Sub"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBlock TextAlignment="Center"
                   FontWeight="Bold"
                   FontSize="32">Make an order!</TextBlock>
        <StackPanel Margin="128 8 128 8">
            <TextBlock>Date</TextBlock>
            <DatePicker DisplayDateStart="{Binding DisplayDateStart}" DisplayDateEnd="{Binding DisplayDateEnd}"/>

            <TextBlock>Date</TextBlock>
        </StackPanel>
    </StackPanel>
</UserControl>
